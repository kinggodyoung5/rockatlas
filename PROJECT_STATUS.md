# ROCK ATLAS 프로젝트 현황

## 프로젝트 위치와 안전 기준선

- 로컬 폴더: `C:\Users\SH\Documents\Codex\rock-atlas`
- 공개 기준 브랜치: `main`
- 현재 대규모 개편 작업 브랜치: `codex/taxonomy-v2`
- 원격 백업 브랜치: `backup/pre-taxonomy-v2-20260719`
- 원격 백업 태그: `pre-taxonomy-v2-20260719`

새 분류 개편은 별도 브랜치에서 진행한다. 검토가 끝날 때까지 `main`, 기존 `catalog.json`, 기존 `genres.json`, 공유된 `#band=` 주소를 삭제하거나 덮어쓰지 않는다.

## 현재 구현 범위

- 공개 밴드 46개, 대표곡 92개, 편집 관계 107개
- 13개 상위 장르, 110개 세부 장르, 24개 분위기 카드
- 모든 밴드는 대표 장르 한 곳에만 노출하고 보조 장르·세부 장르·분위기는 상세와 필터에서 활용
- 메인 탐색 카드 15개: 장르 13개 + 모든 밴드 + 느낌으로 찾기
- 장르별 화면: 실제 등록된 세부 장르, 빠른 분위기, 결과 수, 빈 결과 처리
- 모든 밴드 화면: 이름·스타일 검색, 장르·세부 장르·시대·국가 필터, 정렬
- 느낌으로 찾기: 분위기 최대 3개 조합, 장르·시대·국가 필터, 5% 단위 편집 일치도
- 밴드 상세: 새 대표 장르·세부 장르·대표 분위기·장르 교차점 표시
- 관계 지도: 검수된 관계와 자동 유사도 추천을 별도 표시
- URL 상태 보존: `?view=genre|bands|moods`와 기존 `#band=` 상세 주소 병행
- 대표곡: 영상 임베드 없이 앨범·연도·감상 안내·곡별 외부 링크와 공식 채널로 통일
- 즐겨찾기·최근 탐험 기록·공유 패널·반응형 UI·기본 접근성
- 공개 화면과 Studio 운영 UI의 실제 텍스트는 12px(약 9pt) 이상 유지(축소 미리보기 제외)
- 상세·장르 화면의 “이전 페이지”는 방문 기록과 스크롤 위치를 복원하며, 필터 변경은 기록을 불필요하게 늘리지 않음

## Studio 운영 기능

- 기존 밴드 수정, 새 밴드 초안, CSV 입력, 전체 JSON 백업·복원
- Gemini Gem에 한 번 등록하는 짧은 고정 지침과 결과 붙여넣기·파일 선택을 지원하는 새 밴드 검수함
- JSON 코드 블록 자동 추출, 필드 정규화, 중복 ID·이름·외부 식별자 차단, 쉬운 한국어 오류·경고 표시
- 통과한 밴드만 기존 목록 뒤에 일괄 추가하며 밴드·곡·관계·분류를 강제로 비공개 초안 처리
- 실제 파일 저장 직전에 전체 목록의 중복, 관계 대상, 13장르·110세부장르·24분위기 값을 다시 검사하는 공통 안전장치
- 새 13장르의 대표·보조 장르, 관련 세부 장르, 24개 분위기 점수 편집
- 분류 검수 상태와 운영 메모 관리
- 13개 장르 카드의 이름·표시명·영문명·설명·분위기 설명·색상·순서와 24개 분위기 카드 문구 편집
- Wikidata·MusicBrainz 후보 검색과 선택
- 대표곡·멤버·관계·출처·이미지·공개 상태 편집
- 폰트 업로드, 굵기·이탤릭·색상·히어로 이미지·메인 문구 관리
- 전체 링크·이미지 상태 검사, 휴지통, 최근 20회 카탈로그 백업·복구

Studio의 저장 API는 로컬 개발 서버에서만 동작한다. 정적 배포 사이트에서는 운영 파일을 수정할 수 없다.

## 데이터 검증 현황

- `validate:taxonomy`: 13장르·110세부장르·24분위기, 46/46 밴드 통과
- `validate:data`: 데이터 무결성 오류 없음
- TypeScript 검사 통과
- 프로덕션 빌드 통과
- Commons 이미지 권리 메타데이터 46/46 검수 완료, 자동 진단 경고 없음
- 대표곡은 영상 임베드가 아니라 곡 정보와 외부 링크를 검사하며 92/92 유효 링크 확인
- Studio의 구형 8장르 필드는 접힌 호환 영역으로 이동했고, 새 13장르 분류를 바꾸면 자동 동기화됨
- Studio와 영상 검수 화면을 지연 로딩하도록 분리해 공개 사이트 첫 로딩에서 운영자 코드를 제외함
- 밴드·장르·전체 목록·분위기 화면에 따라 브라우저 제목과 설명, 공유 메타 문구가 자동 변경됨
- 성능 메모: 운영자 코드는 분리됐으나 카탈로그가 포함된 공개 초기 JavaScript가 약 533KB여서 데이터가 크게 늘면 카탈로그 분할을 검토

분위기 점수는 외부 서비스의 객관적 사실값이 아니다. 기존 장르·세부 장르·사운드 설명과 MusicBrainz 커뮤니티 태그를 참고한 **편집 초안**이며, Studio에서 운영자가 검수·수정한다.

## 다음 단계

1. 브라우저에서 새 밴드 검수함의 붙여넣기·검사·강제 초안 흐름을 실제 화면 검수
2. 메인·13장르·전체 밴드·분위기·상세·Studio의 모바일 너비와 키보드 이동 확인
3. 밴드 수가 크게 늘기 전에 이미지 최적화와 카탈로그 로딩 분할 기준 수립
4. 사용자 검토 뒤에만 `main`으로 병합·배포
5. 이후 밴드 데이터와 분위기 분류를 운영자 검수 방식으로 확장

댓글·회원가입, 인기도 순위, 장르당 최소 밴드 수 강제, YouTube 임베드 복원은 현재 범위에 포함하지 않는다.

## 운영자가 수정하는 방법

1. `npm run studio` 실행
2. 로컬 주소에 `?studio=1`을 붙여 열기
3. 새 밴드는 “새 밴드 검수함”의 요청문을 Gemini에 붙여넣고 결과 JSON을 다시 검수함에 붙여넣기
4. 자동 검사 후 통과 항목을 초안 추가하고, 왼쪽 목록에서 소개·분류·분위기·출처 확인
5. 디자인 Studio에서 화면 문구·테마·13장르 카드 수정
6. 자동 진단과 미리보기로 확인
7. “전체 저장” 후 `npm run validate:taxonomy`, `npm run validate:data`, `npm run build` 실행
8. GitHub Desktop에서 간단한 요약으로 커밋하고 Push origin

새 밴드는 `draft`로 시작하므로 검수 전 공개 목록에 나타나지 않는다.

Windows에서는 프로젝트 폴더의 `운영자페이지-열기.bat`를 더블클릭해 5173 포트의 Studio 서버와 운영자 화면을 함께 열 수 있다. `운영자페이지-바로가기.url`은 서버가 이미 실행 중일 때 Studio로 바로 이동한다.
