# ROCK ATLAS 현재 작업 상태

> Codex와 Claude가 교대로 작업할 때 가장 먼저 읽는 인수인계 문서다. 과거 상세 기록은 `docs/PROJECT_HISTORY.md`를 참고한다.

## 1. 작업 시작 규칙

1. 이 파일을 끝까지 읽는다.
2. 현재 브랜치, `git status --short`, 최근 커밋, `git diff`를 확인한다.
3. 미커밋 변경은 운영자 또는 다른 AI의 작업으로 보고 삭제·되돌리기·덮어쓰기를 하지 않는다.
4. `catalog.json`이 미커밋이면 HEAD와 밴드 ID를 비교해 새 데이터가 있는지 먼저 확인한다.
5. 문서와 실제 파일/Git이 다르면 실제 상태를 기준으로 문서를 갱신한다.
6. 운영자 승인 없이 `main` 병합·배포를 하지 않는다.

## 2. 현재 기준

- 갱신: 2026-07-31 KST · Claude
- 작업 브랜치: `codex/taxonomy-v2`
- 최신 커밋: `ce36736` (`feat: operator console UX, banner unification, and catalog data audit`)
- 원격 `main`, `codex/taxonomy-v2`: 위 커밋까지 동기화 완료
- 공개 주소: <https://kinggodyoung5.github.io/rockatlas/>
- 배포: `main` 푸시 후 `.github/workflows/deploy-pages.yml`
- 백업 기준: `backup/pre-taxonomy-v2-20260719`
- `.claude/settings.local.json`은 개인 설정이므로 Git에 포함하지 않는다.

## 3. 2026-07-28 카탈로그 전면 검수

- Studio에서 복구·추가된 카탈로그를 기준으로 총 133개 밴드를 검수했다.
- 기존 초안 25개를 모두 공개 상태로 전환했다.
- 25개 밴드에 Wikimedia Commons 이미지, 저작자, 라이선스, 원본 파일 링크를 채웠다.
- 25개 밴드의 Wikidata·MusicBrainz·공식 YouTube 채널을 대조했다.
- 초안과 기존 공개 밴드에서 검색 링크만 있던 대표곡을 모두 직접 영상 링크로 교체했다.
- 현재 402곡 모두 직접 YouTube 링크가 있다.
- LANY `Malibu Nights`의 접근 제한 영상을 정상 공식 오디오로 교체했다.
- Mamas Gun의 존재하지 않는 것으로 확인된 `Red Light Green Light`, `Looking For John`을 공식 발매곡 `This Is The Day`, `Looking For Moses`로 수정했다.
- U2 `I Still Haven't Found What I'm Looking For`, The Cranberries `Ode to My Family`의 잘못된 제목을 바로잡았다.
- Kings of Convenience의 비어 있던 세부 장르와 시대별 세부 장르를 보완했다.
- Florence + the Machine, Cage the Elephant의 누락된 MusicBrainz 식별자를 채웠다.
- Studio 이력에는 대량 변경 직전 133개 카탈로그 원본이 자동 백업되어 있다.

## 4. 검증 결과

- `validate:data`: 밴드 133, 트랙 402, Wikidata 133/133, MusicBrainz 133/133, Commons 이미지 133/133, 공식 YouTube 133/133, 오류·경고 0.
- `validate:taxonomy`: 13개 상위 장르, 110개 세부 장르, 24개 분위기, 133/133 통과.
- Vitest: 5개 파일, 17개 테스트 통과.
- 프로덕션 빌드: 공개 목록 133개, 상세 JSON 133개, 공유 페이지 133개 생성 및 검증 통과.
- 브라우저 확인:
  - 전체 밴드 화면에 133개 표시.
  - Twin XL 이미지와 `Good`, `Lemonade`, `Problematic` 표시.
  - Mamas Gun 이미지와 수정된 `This Is The Day`, `Looking For Moses` 표시.
- 신규·수정 대표곡 79개 중 oEmbed 실검사에서 78개 통과, 접근 제한 1개는 정상 공식 오디오로 교체 후 통과.
- Commons 직접 이미지 일괄 요청은 요청 제한(HTTP 429)이 있었으나 각 파일은 Commons API에서 파일·저작자·라이선스 정보를 개별 확인했다.

## 5. 이번 변경 파일

- `src/data/catalog.json`: 133개 전체 공개 및 검수 정보 반영.
- `scripts/collect-image-candidates.ts`: 초안만 수집하고 결과 파일로 저장하는 옵션 추가.
- `scripts/search-youtube-videos.ts`: 초안/직접 링크 누락 필터와 결과 파일 저장 옵션 추가.
- `PROJECT_STATUS.md`: 깨진 인코딩을 정리하고 현재 상태로 재작성.
- `.claude/settings.local.json`: 사용자 개인 파일, 커밋 제외.

## 6. 커밋·배포 상태

- 카탈로그 검수 커밋 `9e4d28f`를 `main`과 `codex/taxonomy-v2`에 푸시했다.
- GitHub Pages 실행 `30369179158`: 성공.
- 공개 사이트에서 전체 133개 목록, Twin XL·Mamas Gun 노출, Twin XL 이미지와 3곡을 확인했다.
- 상태 문서 후속 커밋도 두 원격 브랜치에 동기화했다.
- 다음 정확한 단계: 새 운영자 요청이 들어오면 이 문서와 최신 Git 상태를 확인하고 시작한다.

## 7. 고정 제품 방향

- 개발 지식이 없는 운영자가 Studio에서 밴드 추가·수정·검수할 수 있어야 한다.
- 13개 상위 장르, 110개 세부 장르, 24개 분위기 체계를 유지한다.
- 밴드는 대표 장르 한 곳에만 표시하고 교차 장르·분위기는 상세와 필터에서 사용한다.
- 대표곡은 임베드하지 않고 곡 설명과 직접 외부 링크를 제공한다.
- 공개 사이트는 읽기 전용이고 Studio 저장은 로컬에서만 허용한다.
- 기존 `#band=` 주소와 밴드별 공유 페이지를 유지한다.
- 디자인·서버 원칙은 `docs/DESIGN_AND_SERVER_PRINCIPLES.md`를 따른다.

## 8. 다음 개발 후보

- 이번 카탈로그 검수 이후 별도 확정된 다음 기능은 없다.
- 새 작업을 시작할 때 운영자 요청과 Git 상태를 우선 확인한다.
- 대량 밴드 추가 시 후보 수집 → 검수 → Studio 저장 → 데이터 검사 → 빌드 → 커밋 순서를 유지한다.

## 9. 2026-07-28 모바일 장르 카드·Studio 디자인 반영

- YouTube Data API의 `search.list`는 반환 결과 수와 무관하게 요청 1회 비용이 같으므로 후보 8개 설정을 유지했다.
- 모바일 장르 카드에서 숨겨졌던 간략 설명을 13px·최대 2줄로 표시하도록 수정했다.
- 운영자가 Studio에서 저장한 히치하이킹 문구 줄바꿈과 클래식/루츠 록 이미지 위치 변경을 보존했다.
- 업로드 검사, Vitest 17개, 프로덕션 빌드와 공개 데이터 생성 검증이 통과했다.
- 운영자가 이번 변경의 커밋·배포를 명시적으로 승인했다.

## 10. 2026-07-30 Claude 점검·수정 (Codex 대규모 개편 이후)

Codex의 공개 데이터 분리(`generate-public-data.ts`, `publicBands.ts`, 공유 페이지, Vitest 도입)와 133개 카탈로그 전면 검수 이후, 운영자가 제기한 3건의 확인사항과 3건의 수정사항을 처리했다.

**확인사항 (조사만, 코드 변경 없음)**

1. 개러지록(Garage Rock) 세부 장르가 없다 — `taxonomy.v2.json`에는 `garage-punk`, `garage-rock-revival`만 있고 순수 `garage-rock`은 존재하지 않는다. 실제 분류 공백이며, 추가 여부는 운영자 결정이 필요하다.
2. "밴드가 나중에 등록되면 자동으로 연결됩니다" 안내는 실제로 동작하지 않는다 — `bandIntake.ts`가 존재하지 않는 타겟 밴드의 관계를 걸러낼 때 데이터를 어디에도 저장하지 않고, 유일한 후속 장치는 사람이 손으로 관리하는 `docs/PENDING_RELATIONS.md`뿐이다. 안내 문구가 과장돼 있다.
3. 포스트펑크-고딕 장르 설명과 킬러스·카이저칩스 음악의 괴리 — 두 밴드가 속한 `post-punk-revival` 세부 장르가 `post-punk-goth-new-wave` 상위 그룹에 묶여 있는데, 그룹 설명 문구는 진짜 어둡고 음울한 나머지 세부 장르(고딕 록, 다크웨이브, 콜드웨이브)를 기준으로 쓰여 있다. 밴드 분류 자체는 정확하며, 상위 그룹 설명·묶음 구조의 문제다.

**수정사항 (구현 완료)**

1. 관계 양방향 자동 동기화 — `src/types/music.ts`에 `Relation.mirroredFrom?: string` 추가, `StudioPage.tsx`에 `syncMirroredRelation()`을 구현해 `addRelation`/`updateRelation`/관계 삭제 시 상대 밴드에 반대 방향 관계(`influenced-by` ↔ `influenced`, 나머지는 대칭)를 자동 생성·삭제한다. 운영자가 직접 입력한 관계(`mirroredFrom` 없음)는 절대 건드리지 않는다. 브라우저에서 Twin XL → Jonas Brothers 관계 추가/종류 변경 시 상대측에 자동 미러가 정확히 생성됨을 확인했다.
2. 분위기(mood) 유사 표현 인식 개선 — `bandIntake.ts`의 기존 문자열 전체 바이그램 비교를 단어 단위 토큰화(`tokenizeMoodLabel`)와 단어별 최적 매칭(`wordSetSimilarity`)으로 교체해, 어순이 바뀌거나 표현만 다른 문구(예: "음울하고 어두운" ≈ "어둡고 음침한")를 더 잘 잡아낸다. `bandIntake.test.ts`에 관련 테스트 4건 추가, 전체 21개 테스트 통과.
3. 모바일 지도 공유 후 뒤로가기 오류 수정 — `App.tsx`의 `popstate` 핸들러가 라우트만 갱신하고 공유 패널 상태(`shareOpen`)는 그대로 두던 문제였다. 밴드 페이지에서 공유 패널을 연 채로 브라우저 뒤로가기를 누르면 URL은 바뀌지만 `overflow:hidden` 오버레이가 화면에 남아 뒤로가기가 안 되는 것처럼 보였다. `onPopState`에 `setShareOpen(false)`를 추가해 해결. 브라우저에서 재현 후 수정을 확인했다.

**검증 결과**

- `npx tsc -b`: 통과.
- `npm test`: Vitest 5개 파일, 21개 테스트 통과 (기존 17개 + 신규 분위기 테스트 4개).
- `npm run validate:data`: 밴드 133, 트랙 402, 관계 227, 오류 없음.
- `npm run validate:taxonomy`: 13개 상위 장르, 110개 세부 장르, 24개 분위기, 133/133 통과.
- `npm run build`: 공개 목록 133개, 상세 JSON 133개, 공유 페이지 133개 생성 및 검증 통과.
- 브라우저 확인: 양방향 관계 동기화, 지도 공유 후 뒤로가기 모두 실제 동작 확인.
- 테스트 중 `catalog.json`에 남았던 임시 테스트 데이터(Twin XL → Jonas Brothers 관계)는 원래 관계(Twin XL → LANY, shared-scene)로 되돌렸다.

**다음 단계**: 위 3건의 확인사항(개러지록 추가 여부, pending-relations 문구/기능 보완, 포스트펑크-고딕 그룹 재구성 여부)은 운영자 결정 대기 중. 커밋·배포는 운영자 승인 후 진행한다.

## 11. 2026-07-30 확인사항 후속 조치 (운영자 결정 반영)

운영자가 섹션 10의 확인사항 3건에 대해 결정을 내려 후속 구현을 완료했다.

1. **개러지록 세부 장르 추가** — `taxonomy.v2.json`에 `garage-rock`(개러지 록)을 신설해 `indie-britpop-garage` 그룹의 `garage-rock-revival` 옆에 추가했다. 다른 상위 장르에도 "리바이벌/파생 장르는 있는데 기본 장르가 없는" 유사 공백이 있는지 110개 세부 장르 전체를 점검했고, `garage-rock` 외에는 발견되지 않았다(레거시 v1 `genres.json`에만 남아있던 "부기 록"은 현재 어떤 밴드도 사용하지 않아 제외).
2. **자동 보류(pending relations) 시스템 구현** — 안내 문구만 있고 실제로는 관계 데이터를 버리던 기존 동작을 실제로 동작하는 시스템으로 교체했다.
   - `src/types/music.ts`: `PendingRelation` 타입 추가.
   - `src/lib/bandIntake.ts`: 대상 밴드가 아직 없는 관계를 버리지 않고 `IntakeCandidate.pendingRelations`로 보존.
   - `src/components/StudioPage.tsx`: `resolvePendingRelations()`로 새 밴드가 추가될 때마다(일괄 검수함·CSV·수동 새 밴드 저장 모두) 보류 목록을 검사해 대상 밴드가 존재하면 양쪽에 정방향·역방향 관계를 자동 생성. 카탈로그 저장 시 `pendingRelations`를 `catalog.json` 최상위에 함께 저장.
   - `src/components/DataManagerPanel.tsx`: "보류 중인 관계" 진단 패널 추가로 대기 중인 항목을 운영자가 볼 수 있게 함.
   - `server/studioApi.ts`: `/api/studio/catalog` PUT이 `pendingRelations`도 함께 저장하도록 확장.
   - 브라우저에서 A→B(B 미존재) 관계로 밴드 A를 먼저 추가 → "보류 중인 관계 1" 확인 → 밴드 B 추가 → 보류 0으로 감소, A는 `influenced-by → B`, B는 자동 생성된 `influenced ← A`를 정확히 보유함을 실제로 검증했다(테스트 데이터는 저장하지 않고 폐기).
3. **포스트펑크/고딕/뉴웨이브 그룹 설명 수정** — 운영자가 제안한 문구로 교체: 카드 설명(`vibeDescription`) "80년대 감성의 스타일리시한 사운드, 세련된 도시의 록.", 상세 설명(`description`) "펑크의 거친 에너지에 전자음과 댄서블한 리듬을 얹어, 어두운 감성부터 신나는 팝 멜로디까지." 어둡고 음울한 서브장르(고딕 록·다크웨이브·콜드웨이브)와 밝고 신나는 포스트펑크 리바이벌(킬러스·카이저칩스 등)을 모두 포괄하는 문구로, 실제 포스트펑크 장르사(조이 디비전 계열의 음울함과 뉴웨이브 계열의 화려함이 공존)와도 부합한다.

**검증 결과**: `tsc -b`, `npm test`(5개 파일 21개 통과), `validate:data`(133개 밴드 오류 없음), `validate:taxonomy`(111개 세부 장르, 오류 없음), `npm run build`(공개 목록·상세 JSON·공유 페이지 각 133개) 모두 통과. 브라우저에서 개러지록 세부 장르 노출, 포스트펑크 그룹 카드·상세 설명, 보류 관계 자동 연결을 모두 확인했다.

**커밋·배포**: 커밋 `7a6fb3e`(섹션 10 수정사항)와 `0442f95`(섹션 11 후속 조치)를 `codex/taxonomy-v2`에 푸시하고 동일 커밋으로 `main`을 fast-forward했다. GitHub Pages 배포는 `main` 푸시로 자동 트리거된다(gh CLI가 이 환경에 없어 실행 결과는 직접 확인하지 못했다 — 공개 사이트에서 반영을 확인할 것).

## 12. 2026-07-30 운영자 콘솔 2.0 · 배포 준비센터 1단계

운영자와 Codex·Claude의 교차 검토를 바탕으로 다음 개발 순서를 확정했다.

1. 운영자 콘솔 2.0과 안전한 배포 준비센터
2. 기존 수동 보류 관계 이관과 낡은 문서 정리
3. 보류 관계·양방향 관계·인테이크 파이프라인 회귀 테스트 확대
4. 분위기 점수 커버리지 점검 후 히치하이킹 탐색 2.0
5. 규모 증가 뒤 장르 관리 로그·통합 검색·백업 범위 확장

**이번 구현**

- Studio 데이터 화면 최상단에 운영 요약 대시보드를 추가했다.
- 검수 대기 초안, 자동 진단 오류·주의, 자동 연결 대기 관계, 링크 검사 결과, 저장되지 않은 편집 여부를 한눈에 표시한다.
- 기존 `DataManagerPanel`에 중복돼 있던 자동 진단 계산을 `src/lib/studioDiagnostics.ts`의 순수 함수로 분리해 대시보드와 상세 진단이 같은 결과를 사용한다.
- 링크·이미지 검사 결과를 대시보드에 실시간 전달한다. 네트워크 부하를 피하기 위해 전체 링크 검사는 자동 실행하지 않고, 검사 전에는 `미검사`로 명시한다.
- 로컬 Studio 서버에 Git 변경 요약 API를 추가했다. 현재 브랜치, 최신 커밋, Git에 반영할 변경 파일을 보여주며 개인용 `.claude/settings.local.json`은 목록에서 제외한다.
- `배포 전 전체 검사 실행` 버튼을 추가했다. 데이터 검사 → 장르·분위기 검사 → Vitest → 프로덕션 빌드를 고정된 명령으로만 실행하며 커밋·푸시·배포는 하지 않는다.
- 브라우저에 저장되지 않은 편집이 있으면 배포 전 검사 버튼을 비활성화해 디스크의 이전 상태만 검사하는 실수를 방지한다.
- Windows에서 `.cmd`를 직접 실행할 때 발생한 `spawn EINVAL`과 별도 로컬 서버의 Git safe-directory 문제를 실제 브라우저 검증 중 발견해 수정했다.

**검증 결과**

- `npm test`: 6개 파일, 24개 테스트 통과.
- `npx tsc -b`: 통과.
- `npm run build`: 공개 목록·상세 JSON·공유 페이지 각 133개 생성, 메인 전체 카탈로그 선로딩 없음 검증 통과.
- 로컬 Studio 브라우저 검증:
  - 대시보드에 밴드 133개 기준 초안 0, 자동 진단 0, 보류 관계 0, 링크 1,470개 미검사가 표시됨.
  - Git 브랜치와 변경 파일 8개가 정상 표시됨.
  - 배포 전 전체 검사 버튼으로 네 단계를 실제 실행해 모두 통과함.
- 이번 변경은 아직 커밋·푸시·배포하지 않았다. `.claude/settings.local.json` 외의 작업 파일은 운영자 콘솔 관련 변경이다.

**정확한 다음 단계**

- 운영자가 새 콘솔 화면을 확인한 뒤 1단계 범위를 확정한다.
- 후속 작업은 수동 보류 문서의 6건을 새 `pendingRelations` 데이터로 이관하고 `README.md`, `docs/NEXT_STEPS.md`, `docs/PENDING_RELATIONS.md`를 현재 구조에 맞게 정리한다.

## 13. 2026-07-30 보류 관계 이관·회귀 테스트·분위기 커버리지

운영자가 로드맵 4단계까지 연속 진행하도록 승인해 섹션 12의 후속 작업을 완료했다.

### 수동 보류 관계 이관과 문서 정리

- 과거 `docs/PENDING_RELATIONS.md`의 미해결 6건을 실제 카탈로그 상태와 대조했다.
- 이미 대상 밴드가 존재하는 두 건을 즉시 양방향으로 완성했다.
  - Oasis → Blur `shared-scene` 추가. Blur → Oasis 기존 관계는 중복 없이 보존.
  - Theory of a Deadman → Nickelback `influenced-by` 추가. Nickelback → Theory of a Deadman 기존 `influenced` 관계는 중복 없이 보존.
- 아직 대상 밴드가 없는 네 건을 `catalog.json.pendingRelations`로 이관했다.
  - Queen → David Bowie
  - Green Day → The Offspring
  - Green Day → Rancid
  - Bon Jovi → Bryan Adams
- `validate:data`와 Studio 저장 API가 보류 관계의 중복 ID, 출발 밴드, 대상 존재 여부, 자기 관계, 종류·강도를 검사하도록 강화했다.
- `README.md`를 실제 133개 밴드·402곡·229관계·111세부장르 기준으로 갱신했다.
- `docs/NEXT_STEPS.md`를 현재 로드맵으로 재작성하고 `docs/PENDING_RELATIONS.md`는 운영 데이터가 아닌 이관 기록으로 전환했다.

### 회귀 테스트 확대

- `src/lib/relationSync.ts`로 보류 관계 해소와 양방향 미러 동기화를 순수 함수로 분리했다.
- 보류 유지, 대상 추가 후 양방향 생성, 기존 역관계 중복 방지, 관계 대상 변경, 자동 미러 삭제, 수동 관계 보존을 테스트한다.
- Gemini 인테이크가 유사 분위기를 정식 ID로 바꾸고 미등록 대상 관계를 보류함에 보존하는 전체 흐름 테스트를 추가했다.
- Vitest는 7개 파일, 32개 테스트로 증가했다.

### 분위기 커버리지 진단

- `scripts/audit-mood-coverage.ts`와 `npm run audit:moods`를 추가했다.
- 새 밴드에도 같은 기준이 적용되도록 Studio 배포 전 검사에 분위기 진단 단계를 포함했다.
- 현재 결과:
  - 133개 밴드 모두 분위기 3개 이상.
  - 3점 이상 핵심 분위기가 없는 밴드 0개.
  - 24개 분위기 전부 사용.
  - 밴드당 평균 4.87개 분위기.
  - 히치하이킹 방향 후보: 더 무겁게 59, 더 몽환적으로 22, 더 대중적으로 107, 더 실험적으로 34.
  - 희소 항목은 `잔잔하고 느린` 3개, `우주적이고 환각적인` 4개지만 방향 탐색 후보 수는 충분하다.
- 외부 검색과 점수 보충은 필요하지 않다고 판단했다. 카탈로그 구성상 희소한 항목을 숫자만 맞추려고 임의로 추가하면 분류 품질이 오히려 낮아지므로 기존 검수 점수를 유지했다.

### 최종 검증

- `npm test`: 7개 파일, 32개 통과.
- `validate:data`: 밴드 133, 트랙 402, 관계 229, 보류 관계 4, 오류 없음.
- `validate:taxonomy`: 13개 상위 장르, 111개 세부 장르, 24개 분위기, 133/133 통과.
- `audit:moods`: 히치하이킹 2.0 방향 탐색 커버리지 충족.
- `npm run build`: 공개 목록·상세 JSON·공유 페이지 각 133개 생성 및 검증 통과.
- 로컬 Studio에서 보류 관계 4건 표시와 5단계 배포 전 검사를 실제 실행해 모두 통과했다.
- 이번 연속 작업은 아직 커밋·푸시·배포하지 않았다.

**정확한 다음 단계**: 운영자 승인 후 히치하이킹 탐색 2.0의 방향별 추천 계산과 추천 이유·여행 경로 UI 설계를 시작한다.

## 14. 2026-07-30 히치하이킹 탐색 2.0 · 로컬 전체 백업

운영자가 사이트의 핵심 기능으로 히치하이킹 2.0 구현을 승인했다. 복잡한 추천 설정 화면 대신, 현재 밴드와 관련 있는 방향 중 하나를 고르고 이유가 보이는 다음 밴드 3개만 선택하는 구조로 구현했다.

### 복구 기준과 반복 가능한 백업

- 구현 시작 전 현재 미커밋 작업본 전체 ZIP과 Git 전체 이력 bundle을 `C:\Users\SH\Documents\Codex\rock-atlas-backups`에 생성했다.
  - `rock-atlas-worktree-20260730-191032.zip`
  - `rock-atlas-history-20260730-191032.bundle`
- 프로젝트 루트에 `프로젝트-백업-만들기.bat`를 추가했다. 비개발자 운영자가 더블클릭하면 언제든 새 타임스탬프 백업 두 개를 같은 방식으로 만든다.
- 작업본 ZIP은 `node_modules`, `dist`, `.git`을 제외하지만 미커밋 소스와 `.env.local`을 포함한다. Git bundle이 전체 커밋 이력을 별도로 보존한다. ZIP은 API 설정을 포함할 수 있으므로 공개 업로드 금지다.

### 유지보수 가능한 추천 엔진

- `src/config/hitchhiking.ts`에 방향 후보군과 분위기 가중치를 단일 설정으로 분리했다.
  - 더 무겁게
  - 더 몽환적으로
  - 더 대중적으로
  - 더 실험적으로
  - 더 빠르게
  - 더 어둡게
  - 더 따뜻하게
  - 더 그루비하게
  - 더 웅장하게
- 모든 방향을 모든 밴드에 공통 노출하지 않는다. 현재 밴드가 3점 이상의 핵심 분위기를 실제로 갖거나, 직접 검수된 관계를 통해 자연스럽게 건너갈 수 있고, 음악적으로 가까운 후보가 3개 이상인 방향만 최대 4개 노출한다.
- 가중치가 낮은 보조 분위기는 추천 순위만 다듬고 방향 버튼을 단독 생성하지 않는다.
- `src/lib/hitchhiking.ts`가 방향 일치도, 기존 밴드와의 장르·세부장르·분위기 연속성, 편집 관계, 방문 여부를 함께 계산한다.
- 특정 밴드명이나 장르별 예외를 사용하지 않는다. 공개 상태와 taxonomy v2 분위기 점수가 있는 새 밴드는 추가 즉시 9개 방향 후보군의 공통 선별 규칙을 적용받는다.
- 방향 변화량은 무한히 크게 보상하지 않고 한 걸음 분량에서 상한을 둔다. 예를 들어 The Beatles에서 `더 무겁게`를 선택할 때 극단적 메탈부터 보여주지 않고 The Who·Led Zeppelin·The Rolling Stones 같은 연속성 있는 후보가 먼저 나온다.
- 이미 방문한 밴드는 새 후보 뒤로 보낸다. `다른 후보 3개`는 무작위가 아니라 계산 순서의 다음 묶음을 보여줘 결과가 예측 가능하다.
- 기존 상세 하단의 포괄적인 `자동 유사도 추천`은 제거하고, 직접 검수한 관계 지도는 `편집된 계보와 장면`으로 명확히 분리했다.

### 사용자 흐름과 공유

- 밴드 상세에 `다음에는 어떤 소리로 갈까요?` 패널을 추가했다.
- 밴드별 관련 방향을 고르면 사진, 밴드명, 핵심 분위기, 음악적 연결 이유가 있는 후보 3개가 표시된다.
- 이동 경로는 브라우저에 최대 12단계까지 자동 저장된다.
- `여기서 새로 시작`으로 현재 밴드를 새 출발점으로 만들 수 있다.
- 두 단계 이상 이동하면 `여정 공유`가 나타나며, 밴드 ID와 이동 방향을 담은 URL로 같은 경로를 복원한다.
- 브라우저 뒤로가기·앞으로가기도 각 페이지의 여행 경로 상태를 함께 복원한다.

### 검증

- Vitest: 8개 파일, 38개 테스트 통과.
  - 133개 공개 밴드 모두 관련 방향 1개 이상, 각 노출 방향마다 서로 다른 후보 3개 보장.
  - 결정적 추천 순서, 방문 후보 후순위, 여정 URL 왕복·비정상 입력 방어 확인.
- `npx tsc -b`: 통과.
- `validate:data`: 밴드 133, 트랙 402, 관계 229, 보류 관계 4, 오류 없음.
- `validate:taxonomy`: 13개 상위 장르, 111개 세부 장르, 24개 분위기, 133/133 통과.
- `audit:moods`: 9개 방향의 원천 후보 수는 더 무겁게 85, 더 몽환적으로 23, 더 대중적으로 110, 더 실험적으로 36, 더 빠르게 79, 더 어둡게 48, 더 따뜻하게 66, 더 그루비하게 90, 더 웅장하게 100. 외부 검색이나 점수 보충 없이 충분.
- 프로덕션 빌드: 공개 목록·상세 JSON·공유 페이지 각 133개 생성 및 메인 전체 카탈로그 선로딩 없음 검증 통과.
- 실제 브라우저:
  - 데스크톱 밴드별 관련 방향·후보 3개 배치와 추천 이유 확인.
  - 390×844 모바일에서 방향 2×2, 축소 후보 카드와 가독성 확인.
  - 방향 이동, 여정 공유 URL 복원, 뒤로가기·앞으로가기 시 경로 복원 확인.

### 2026-07-30 방향 관련성 보완

- 운영자 검수에서 Children of Bodom에 `더 몽환적으로`가 노출되는 문제가 발견됐다. 원인은 추천 후보만 밴드별로 계산하고 방향 버튼 네 개는 공통 노출한 초기 설계였다.
- Children of Bodom의 실제 점수는 `빠르고 질주하는 5`, `강렬하고 공격적인 5`, `웅장하고 영화적인 4`, `복잡하고 테크니컬 4`이며 몽환 계열은 0이다.
- 방향 후보군을 9개로 확장하고 밴드마다 관련 방향만 최대 4개 선별하도록 변경했다. Children of Bodom에는 현재 `더 무겁게·더 빠르게·더 웅장하게·더 실험적으로`만 표시된다.
- 후보 역시 같은 대표 장르, 대표/보조 장르 교차, 공통 세부 장르, 직접 검수 관계 중 하나가 있어야 한다. 이 조건을 만족하는 후보가 3개 미만이면 해당 방향 자체를 숨긴다.
- 이 보완으로 Children of Bodom의 몽환 경로와 Mew·King Gizzard처럼 분위기 단어만 일부 겹치던 과도한 장르 점프를 제거했다.

### Git 상태와 다음 행동

- 섹션 12~14의 연속 작업은 아직 커밋·푸시·배포하지 않았다.
- `.claude/settings.local.json`은 개인 파일이므로 계속 Git 대상에서 제외한다.
- **정확한 다음 단계**: 운영자가 로컬 히치하이킹 화면과 추천 결과를 확인한다. 수정 요청이 없으면 현재 운영자 콘솔·보류 관계·테스트·히치하이킹·백업 변경을 한 번에 정리해 커밋·푸시·배포한다.

## 15. 2026-07-30 새 밴드 검수함 2.0 · Gemini 역할 축소

운영자가 모든 외부 검색 버튼을 반복해서 누르지 않으면서도 Gemini의 잘못된 ID·링크와 그럴듯한 오정보를 그대로 공개하지 않도록 새 밴드 인테이크 흐름을 개편했다.

### 구현 내용

- Gemini Gem 지침을 v4로 교체했다.
  - Gemini는 소개·스타일·장르·분위기·멤버·대표곡 정보와 `sounds-like`/`shared-scene` 관계 후보만 작성한다.
  - 자주 틀리던 Wikidata ID, MusicBrainz ID, Commons 파일명, YouTube 영상·채널 URL은 요청하지 않는다.
  - `influenced-by`·`influenced`·`evolution`은 출처 검증이 필요한 관계이므로 Gemini 출력 대상에서 제외했다.
- 검수함의 기본 동작을 `전체 자동 확인` 한 번으로 통합했다.
  - 밴드명을 기준으로 Wikidata와 MusicBrainz를 자동 검색한다.
  - 이름·별칭·음악 그룹 여부·국가·결성지·결성 연도를 종합해 후보 순위를 계산한다.
  - Wikidata의 MusicBrainz ID(P434)가 실제 MusicBrainz 후보와 같으면 두 항목을 자동 확정한다.
  - 결성 연도·국가·결성지를 별도로 비교하고, 충돌하는 항목만 운영자에게 보여준다.
  - 동명 아티스트 등 확신이 부족한 경우에만 상위 후보 3개 중 하나를 고르게 한다.
  - 자동 선택된 경우에도 `다른 후보가 맞다면 변경`을 펼쳐 운영자가 언제든 상위 후보로 교체할 수 있다.
- 정확한 Wikidata 항목에서 Wikipedia, 공식 YouTube 채널 ID, Commons 이미지 파일을 안전하게 자동 연결한다.
  - Commons 이미지는 파일 존재뿐 아니라 저작자·라이선스·원본 주소까지 다시 확인한 뒤에만 채운다.
  - 검색 결과는 로컬 Studio 서버에서 한 시간 캐시해 같은 밴드 재검색과 외부 API 낭비를 줄였다.
  - 여러 밴드 묶음은 MusicBrainz 요청 제한을 고려해 순차 검증한다.
- Gemini가 직접 영상 URL을 만들지 않으므로 대표곡은 곡 정보와 안전한 YouTube 검색 링크로 먼저 보존한다. 깨진 가짜 직접 링크가 들어오는 경로를 줄였다.
- 외부 신원 검증과 편집 내용 승인을 분리했다.
  - 두 데이터베이스가 일치해도 소개문·스타일·장르·분위기·관계 후보까지 자동으로 사실 확정하지 않는다.
  - 검수 카드에서 이 네 항목을 한 번에 펼쳐 보고 `위 소개·분류·관계 후보를 읽고 공개로 추가`를 한 번 체크해야 공개 준비 완료가 된다.
  - 이미지 권리나 필수 출처가 남아 있으면 체크해도 공개되지 않고 비공개 초안으로 안전하게 유지한다.
- 영향·계보 관계가 기존 형식으로 들어오더라도 근거 URL이 없으면 공개 차단 경고를 표시한다.
- 기존 수동 Wikidata·MusicBrainz 검색 화면도 새 상세 후보 정보(유형·결성지·국가·결성 연도)를 함께 보여주도록 보강했다.

### 검증

- `npx tsc -b`: 통과.
- Vitest: 9개 파일, 43개 테스트 통과.
  - 교차 연결 자동 확정, 동명 개인 아티스트 후순위, 결성 연도 충돌, 외부 출처 자동 입력, Gemini v4에서 취약한 ID·URL 제거를 검사한다.
- `validate:data`: 현재 미커밋 카탈로그 150개 밴드, 오류 없음(초안·검색 링크 관련 기존 경고 76개).
- `validate:taxonomy`: 13개 상위 장르, 111개 세부 장르, 24개 분위기, 150/150 통과.
- 프로덕션 빌드: 공개 상태 133개 기준 목록·상세 JSON·공유 페이지 각 133개 생성 및 검증 통과.
- 로컬 브라우저에서 Rush 시험 JSON으로 실제 확인:
  - Wikidata Q203871 ↔ MusicBrainz `534ee493-bfac-4575-a44a-0ae41e2c3fe4` 자동 교차 연결.
  - 결성 연도 1968, 국가 CA, 결성지 Toronto 일치.
  - Wikidata Commons 사진과 Public domain 권리 자동 입력.
  - 외부 확인 뒤에는 `내용 확인만 남음`, 운영자 체크 뒤에는 `공개 준비 완료`로 정확히 전환.
  - 시험 데이터는 저장하지 않고 검수함에서 비웠으며 브라우저 콘솔 오류 없음.

### Git 상태와 다음 단계

- 기존 미커밋 운영자 콘솔·보류 관계·히치하이킹·150개 카탈로그 변경을 그대로 보존한 위에 이번 기능을 추가했다.
- 이번 변경도 아직 커밋·푸시·배포하지 않았다.
- **정확한 다음 단계**: 운영자가 로컬 Studio의 검수함 2.0을 확인한다. 승인하면 현재 누적 변경 전체를 다시 검사한 뒤 커밋·푸시·배포한다. 기존 17개 비공개 초안은 검수함 기능 구현 과정에서 수정하거나 공개하지 않았다.

## 16. 2026-07-30 검수함 2.0 배포 준비

- 대표곡 제목은 Gemini가 초안에 넣고, 정확한 영상은 기존 Studio의 곡별 `영상 검색 → 후보 선택` 기능으로 운영자가 확정하는 현행 흐름을 유지한다.
- 여러 대표곡을 한꺼번에 YouTube에서 검색하는 기능은 단순 클릭 수 절감용 제안이었으며, 운영자 요청에 따라 이번 구현과 다음 필수 작업에서 제외했다.
- R.E.M.은 카탈로그에서 `published` 상태이며 이번 빌드의 공개 밴드 목록에 포함된다.
  - 대표곡 3개는 안전한 YouTube 검색 링크 상태다.
  - 직접 영상 ID는 Studio의 기존 곡별 검색에서 운영자가 후보를 선택할 때 확정된다.
- 배포 전 R.E.M.의 Wikimedia Commons 이미지 원본 주소와 출처 목록 주소가 불일치하던 문제를 발견했다.
  - 검증된 Commons 출처를 카탈로그에 추가했다.
  - 앞으로 검수함이 Commons 사진을 자동 채울 때 동일한 출처도 함께 추가하도록 수정했다.
- 최종 사전 검사:
  - Vitest: 9개 파일, 43개 테스트 통과.
  - TypeScript 검사 통과.
  - `validate:data`: 151개 밴드, 456개 대표곡, 257개 관계, 오류 없음.
  - `validate:taxonomy`: 13개 상위 장르, 111개 세부 장르, 24개 분위기, 151/151 통과.
  - `audit:moods`: 24개 분위기 모두 최소 커버리지 충족.
  - 프로덕션 빌드: 공개 밴드 134개와 밴드별 상세·공유 페이지 134개 생성, 전체 카탈로그 선로딩 없음 검증 통과.
- `.claude/settings.local.json`은 개인 로컬 설정이므로 계속 커밋 대상에서 제외한다.
- 첫 배포 실행은 GitHub의 깨끗한 Linux 환경에 `src/data/generated/`가 아직 없어서 회귀 테스트가 시작되지 못했다.
  - `pretest`에서 공개 데이터를 먼저 생성하도록 고쳐, 새 PC·새 계정·GitHub Actions에서도 같은 순서로 재현 가능하게 했다.
  - 수정 후 테스트 43개와 프로덕션 빌드를 다시 통과했다.

## 17. 2026-07-31 Claude 후속 UX·데이터 검수 (Codex 라이브 세션 이후)

Codex가 운영자 콘솔·검수함 2.0·히치하이킹 2.0을 라이브 Studio 세션에서 165개까지 카탈로그를 키운 뒤, Claude가 UX 요청과 데이터 품질 검수를 이어서 처리했다. 라이브 세션 도중에는 `catalog.json` 직접 수정을 보류했고, 세션 종료 확인 후에만 파일을 직접 고쳤다(동시 저장 충돌로 앞서 곡 설명 수정 일부가 지워진 사고 이후 확립한 규칙).

**Studio UX 추가**
- 곡 설명(guide) 없는 대표곡만 모아서 바로 해당 밴드로 이동하는 전용 진단 목록을 `DataManagerPanel`과 대시보드에 추가했다(`getMissingTrackGuides`).
- 히치하이킹 패널과 "편집된 계보와 장면" 섹션의 모든 문구(제목·설명·버튼·9개 방향별 라벨)를 `siteContent.bandDetailCopy`로 옮겨 Studio 디자인 화면에서 편집 가능하게 만들었다.
- 검증된 연결이 없는 밴드는 빈 칸 대신 안내 문구를 보여주도록 `RelationMap`을 수정했다.
- 헤더·푸터의 "RA" 로고 마크를 삭제하고, 네온사인 워드마크 이미지 하나로 통일한 배너로 교체했다(`theme.bannerImageUrl`).
- 운영자 요청에 따라 "편집된 계보와 장면" → "실제로 얽혀 있던 밴드들", 히치하이킹 제목 "다음에는 어떤 소리로 갈까요?" → "다음 차는 어디로 갈까요?" 등 문구를 히치하이커 콘셉트에 맞게 다시 썼다.

**데이터 검수**
- 곡 설명·앨범·연도가 비어 있던 22개 대표곡(Arch Enemy, KISS, Van Halen, Vampire Weekend, The Strokes, U2 등)을 실제 앨범·발매년도로 조사해 채웠다.
- **시대별 분류 조사 — 처음엔 잘못 진단했다가 정정함**: 처음에는 "활동 기간 내내 시대 태그가 하나뿐인 것"을 버그로 보고 `activeYears`를 파싱해 활동한 모든 10년 단위에 태그를 만들도록 고쳤으나(119개 밴드 소급 수정 포함), 운영자가 원래 규칙을 다시 확인해준 뒤 이 변경을 전부 되돌렸다. **원래 설계**: 장르 변화가 없으면 결성 연도 기준 시대 하나만 쓰고(`eraTags: [{ era: eraFromYear(formed), ... }]`), 장르가 실제로 바뀌면(Coldplay처럼 얼터너티브 → 포스트 브릿팝 → 팝 록 → 팝) Studio의 "시대별 분류" 수동 입력란(`시대 | 세부 장르 | 설명` 형식)에 운영자·AI가 직접 조사해서 여러 줄을 적는다. 이 수동 입력은 `bandIntake.ts`의 자동 검수함 파이프라인이 생긴 첫 커밋(`2bbb5bf`)부터 지금까지 한 번도 자동화된 적이 없다 — 즉, 코드 회귀(regression)가 아니라 "카탈로그 전면 검수" 같은 느린 수작업 단계에서 매번 사람이 직접 채워온 항목이며, 최근 빠른 일괄 등록(검수함 2.0)으로 늘어난 밴드들은 이 수작업이 아직 이뤄지지 않았을 뿐이다. 기존 밴드도 Studio에서 얼마든지 이 필드를 다시 편집해 장르 변화를 추가할 수 있다 — 다만 실제로 장르가 바뀌었는지는 밴드별로 조사해야 하므로 자동으로 채우지 않았다.
- 이 과정에서 Yungblud의 결성 연도가 1997(실제로는 본인의 출생 연도)로 잘못 기록된 것을 발견해 2018년(실제 활동 시작)로 수정했다.
- 이미지 권리 검수 없이 공개 상태였던 오류 3건을 발견해 수정했다: Equilibrium·Generation Axe는 이미지 출처가 아예 없어 초안으로 되돌렸고, Chickenfoot은 Commons 파일 메타데이터에서 누락된 저작자 정보를 보완했다.
- **자동 미러 관계 문구 개선**: 상대 밴드에 자동 생성되던 관계가 "OO 쪽에서 추가한 연결 관계 (자동 생성 · 필요하면 다듬으세요)"라는 내용 없는 문구만 달고 있던 문제를 고쳤다. 원본 관계에 실제 근거 문구가 있으면 그 문구를 그대로 재사용하고("근거 문구 (OO 쪽 관계에서 자동 연결됨)"), 원본이 빈 플레이스홀더일 때만 기존 일반 문구로 대체한다(`src/lib/relationSync.ts`의 `mirroredNote`).

**검증 결과**
- `npx tsc -b`, `npm test`(9개 파일 51개 통과), `validate:data`(165개 밴드 오류 0 · 경고 19개, 전부 초안·검색링크 관련 정상 경고), `validate:taxonomy`(111개 세부 장르 165/165), `audit:moods`(커버리지 충족), `npm run build`(공개 163개 목록·상세 JSON·공유 페이지 생성 및 검증 통과) 모두 통과.
- 브라우저에서 Rush 상세 페이지의 시대별 장르 변화가 "1960년대~2010년대"로 정상 표시됨을 확인했다.

**커밋·배포**: 운영자 승인에 따라 이번 세션의 누적 변경(섹션 12~17)을 한 번에 커밋·푸시하고 `main`에 배포한다.

## 18. 2026-08-01 장르 변화 규칙 복구·정밀화

운영자가 요청한 원칙은 다음과 같이 확정했다.

- 장르 변화가 없으면 결성 시대의 핵심 장르 한 줄만 저장하고 상세 화면에서는 `결성 시대~현재`로 표시한다.
- 여러 시대를 기록하는 것은 앨범 한 장의 일시적 실험이 아니라 이후에도 이어진 장기 노선 변화가 확인된 경우뿐이다.
- 보컬·멤버 교체 자체는 변화 근거가 아니며, 실제 앨범의 편곡·리듬·악기·프로덕션 변화가 지속됐는지로 판단한다.
- 변화마다 기준 앨범명과 연도를 `note`에 남긴다.

**코드 보완**

- Claude가 추가한 `eraGenreShifts` 인테이크를 검토해 설명 `note`가 유실되던 문제를 수정했다.
- 시대가 두 줄 이상이어도 장르 조합이 같으면 변화로 인정하지 않고 안전한 단일 시대 기본값으로 되돌린다.
- Gemini가 제안한 변화가 유효하지 않거나 기준 앨범 설명이 빠지면 검수함 경고가 발생하고 자동 공개되지 않는다.
- 검수함의 최종 승인 화면에서 시대별 장르·기준 앨범 설명을 한 번에 읽을 수 있게 했다.
- Gemini Gem 지침은 v5로 갱신했다. 모든 언급 앨범을 `앨범명(연도)`로 쓰고, 실제 장기 변화에만 `eraGenreShifts`를 작성하며 각 변화에 기준 앨범을 쓰도록 했다. Studio 버튼·복사 완료 문구·운영 문서의 버전도 v5로 통일했다.
- 안정적으로 같은 장르를 유지한 밴드와 활동 종료 밴드의 표시 규칙을 회귀 테스트로 고정했다.

**검증·반영한 밴드**

- Coldplay와 Judas Priest의 기준 앨범 설명을 보완했다.
- David Bowie, Nightwish, Epica, Linkin Park, My Chemical Romance, Paramore, Fall Out Boy, Talking Heads, The Cure, Blur, Deftones, Red Hot Chili Peppers에 장기 노선 변화와 기준 앨범을 기록했다.
- Nightwish는 보컬 교체를 장르 변화로 간주하지 않고 오케스트레이션·포크·프로그레시브 구성이 지속된 앨범 시기만 분리했다.
- Weezer는 앨범마다 스타일 실험은 많지만 한 방향으로 굳어진 변화가 아니므로 단일 장기 노선으로 유지했다.

**작업 중 발견해 함께 해결한 오류**

- Beck의 Commons 사진에 빠졌던 저작자 `Oliver Mark`를 원본 메타데이터로 확인해 보완했다.
- Massive Attack의 구형 세부 장르와 시대 태그가 비어 배포 검사가 실패하던 문제를 해결했다. taxonomy에 `trip-hop`을 추가하고 트립합·일렉트로니카 정보를 채웠다.
- 기존 다중 시대 밴드 중 기준 앨범 설명이 없는 항목은 `validate:data` 경고로 보이게 했다. 현재 남은 항목은 오류가 아니라 후속 정밀화 대상이다.

**최종 검사**

- Vitest: 10개 파일, 55개 테스트 통과.
- `validate:data`: 밴드 187개, 대표곡 570개, 관계 313개, 무결성 오류 없음.
- `validate:taxonomy`: 상위 장르 13개, 세부 장르 112개, 분위기 24개, 187/187 통과.
- TypeScript 검사 통과.
- 프로덕션 빌드: 공개 밴드 165개와 상세 JSON·공유 페이지 165개 생성 및 검증 통과.

**Git 상태와 정확한 다음 단계**

- 이번 작업 전에 Claude가 추가한 미커밋 밴드·관계 데이터와 이번 장르 변화 보완이 같은 작업 트리에 함께 있다. 기존 변경을 덮어쓰거나 되돌리지 않았다.
- `.claude/settings.local.json`은 계속 커밋 대상에서 제외한다.
- 이번 변경은 아직 커밋·푸시·배포하지 않았다. 운영자가 결과를 확인하고 명시적으로 요청하면 현재 누적 변경 전체를 커밋·푸시·배포한다.

## 19. 2026-08-01 근거 우선 인테이크 · 기존 카탈로그 재검수함

Gemini가 자유 검색으로 ID·연도·앨범·관계를 함께 만들던 구조를 줄이고, Studio가 구조화 자료를 먼저 확보한 뒤 Gemini는 설명과 분류 초안만 작성하도록 운영 흐름을 보완했다.

**운영자용 새 흐름**

- 새 밴드 검수함 상단에 `밴드명으로 검증 자료 만들기`를 추가했다. 이름만 넣으면 Wikidata와 MusicBrainz 후보를 함께 찾고, 두 출처가 직접 연결되는 항목을 우선 선택한다.
- 동명이인 가능성이 있으면 상위 후보 세 개만 보여주며 운영자가 한 번 눌러 확정한다.
- 선택 뒤 결성 연도·국가·결성지·정규 앨범·멤버 관계를 실제 API에서 가져와 `검증 자료를 Gemini에 보낼 문장`으로 한 번에 복사한다. 이 단계는 카탈로그를 자동 저장하지 않는다.
- Gemini Gem 지침을 v6으로 갱신했다. Studio 검증 자료를 우선하고, 외부 ID·이미지·YouTube URL을 생성하지 않으며, 관계는 항상 빈 배열로 둔다. 실제 장기 장르 변화만 기준 앨범과 함께 제안하게 했다.
- MusicBrainz의 Person 생년을 솔로 아티스트의 활동 시작 연도로 오인하지 않도록 Group 항목에만 결성·종료 연도를 적용한다.

**기존 카탈로그 검수함**

- 데이터 관리 화면에 `외부 사실 정밀 검수`를 추가했다. 현재 밴드 한 건 또는 전체 187개를 순차 검사할 수 있다.
- ID 존재 여부, 두 데이터베이스의 이름·별칭, Wikidata의 MusicBrainz 직접 연결, 결성 연도·국가·결성지·활동 종료, 대표곡 앨범·연도를 비교한다.
- 전체 검사는 MusicBrainz 요청 제한에 맞춰 천천히 진행하며 중단·재개할 수 있고, 결과는 로컬 브라우저에 보관한다.
- 일치 항목은 접고 차이만 보여준다. `외부 값 적용`은 편집본에만 넣으며, 운영자가 확인하고 전체 저장하기 전에는 파일을 바꾸지 않는다.
- MusicBrainz에 멤버 관계가 없다는 사실은 멤버가 틀렸다는 뜻이 아니므로 삭제 오류가 아닌 참고 정보로만 처리한다.
- MusicBrainz 연결 앨범이 API 상한 25개에 도달하면 누락 앨범을 오류로 단정하지 않는다.

**187개 기존 데이터 외부 재검사 결과**

- 187개 전체를 Wikidata·MusicBrainz에 다시 조회했다. 모든 밴드가 현재 유효한 Wikidata ID와 MusicBrainz ID를 갖는다.
- 실제로 잘못 연결돼 있던 Rhapsody를 Wikidata `Q193174`, MusicBrainz `4e0dffde-ad2d-45b7-9c75-d57ce55de061`로 수정했다.
- 누락돼 있던 Machine Gun Kelly, Alestorm, Simple Plan의 Wikidata ID와 Machine Gun Kelly의 MusicBrainz ID를 보완했다.
- 외부 연도와 다른 항목은 자동 덮어쓰지 않았다. 전신 밴드·이름 변경·솔로 활동 시작 기준에 따라 판단이 달라지는 편집 사안이므로 새 검수함의 확인 대상으로 남긴다.

**최종 검증**

- Vitest: 11개 파일, 58개 테스트 통과.
- `validate:data`: 밴드 187, 트랙 570, 관계 313, 보류 관계 50, Wikidata 187/187, MusicBrainz 187/187, 무결성 오류 없음.
- `validate:taxonomy`: 상위 장르 13개, 세부 장르 112개, 분위기 24개, 187/187 통과.
- 프로덕션 빌드: 공개 밴드 165개와 상세 JSON·공유 페이지 165개 생성, 메인 전체 카탈로그 선로딩 없음 검증 통과.
- 실제 Studio 화면에서 R.E.M. 이름 검색으로 정확한 두 외부 항목, 정규 앨범 25개, 멤버 관계 4개와 Gemini용 검증 문장 생성 확인.
- 실제 Studio 화면에서 현재 선택 밴드 정밀 검사, 결과 집계와 로컬 보관 동작 확인.

**Git 상태와 정확한 다음 단계**

- 기존 Claude 작업과 이번 변경은 같은 미커밋 작업 트리에 함께 있으며 사용자 변경을 되돌리지 않았다. `.claude/settings.local.json`은 개인 파일이므로 계속 Git 대상에서 제외한다.
- 이번 변경은 아직 커밋·푸시·배포하지 않았다.

- **정확한 다음 단계**: 운영자가 로컬 Studio에서 새 밴드명 조사와 기존 카탈로그 검수 패널을 확인한다. 승인 후 누적 변경 전체를 커밋·푸시·배포한다. 이후에는 검수함의 `확인 필요` 결과를 운영자가 한 건씩 비교 적용하되, 외부 값의 일괄 자동 덮어쓰기는 하지 않는다.

## 20. 2026-08-01 Gemini 빈 앨범명·멤버 상태 모순 자동 보정

- 새 v6 Gem에서 Sepultura 소개글이 `(1989), (1993), (1996) 등의 앨범`처럼 앨범명 없이 출력되는 실제 사례를 재현했다.
- Gem 지침에 빈 앨범명 금지, 출력 전 자체 점검, 올바른 예시를 추가했다.
- 지침 준수에만 의존하지 않도록 검수함에 결정적 보정 규칙을 추가했다.
  - 대표곡에 앨범명·연도가 있으면 빈 연도 나열을 대표곡 앨범 목록으로 자동 교체하고 정보 메시지를 표시한다.
  - 대표곡에도 앨범명이 없으면 내용을 추측하지 않고 `missing-summary-album-names` 경고로 자동 공개를 막는다.
- 같은 시험 데이터에서 Derrick Green이 `former`이면서 `1997–현재`로 적힌 모순도 발견했다. `former + 현재/present` 조합은 `current`로 자동 보정하고 대상 멤버를 알려준다.
- 제공된 Sepultura 형태를 바탕으로 자동 보완·차단·멤버 상태 보정 회귀 테스트를 추가했다.
- 해당 테스트 19개 통과, TypeScript와 프로덕션 빌드 통과. 아직 커밋·푸시·배포하지 않았다.

## 21. 2026-08-01 세부 장르 표기 허용·taxonomy 확장

Gemini가 같은 장르를 다른 음역·철자로 출력하면 곧바로 제외되던 문제를 수정했다.

**입력 정규화**

- 세부 장르도 분위기처럼 정식 ID·한글명·영문명·관리 별칭을 함께 인식한다.
- 공백·하이픈·대소문자에 더해 `락/록`을 통일하고, 길이가 충분한 명칭은 한 글자(긴 영문명은 최대 두 글자) 오타까지 유일한 후보일 때만 허용한다.
- `사이케델릭/사이케딜릭/싸이키델릭 록`, `가라지/개러지 록`, `쓰래시/스레시 메탈`, `슈게이징`, `멜데스`, `트립 홉` 등 반복 가능성이 높은 음역 차이를 명시적 별칭으로 추가했다.
- 자동 변환된 항목은 `표기·철자 차이를 정식 세부 장르로 바꿨습니다`라는 정보 메시지로 원문과 결과를 함께 보여준다.
- 어떤 정식 항목과도 안전하게 연결하지 못한 용어는 `허용되지 않아 제외`라고 단정하지 않고 검토 대상으로 표시한다.
- `펑크 메탈`처럼 punk와 funk 중 뜻이 갈리는 표기는 의도적으로 자동 매칭하지 않는다.
- 같은 변환기를 Gemini JSON과 CSV 일괄 입력에서 공통 사용한다.

**taxonomy 보완**

- 세부 장르를 112개에서 140개로 확장했다.
- 추가 항목: 로큰롤, 하트랜드 록, 서프 록, 로커빌리, 인디 포크, 파워 팝, 포크 팝, 애시드 록, 사이키델릭 팝, 크라우트록, 아방가르드 메탈, 멜로딕 하드코어, 스크리모, 프로토 펑크, 라이엇 걸, 포크 펑크, 로파이 록, 챔버 팝, 매드체스터, 에테리얼 웨이브, 포스트 메탈, 스토너 메탈, 드론 메탈, 블랙게이즈, 심포닉 블랙 메탈, 랩 록, 훵크 메탈, 젠트.
- Gem 지침이 자유 번역명만 요구하던 문제를 고쳐, 140개 허용 세부 장르 ID 전체를 직접 제공하고 가능한 한 ID를 그대로 출력하게 했다.
- 이 변경은 운영자가 기존 v6과 구분할 수 있도록 `Gemini Gem 지침 v7`로 올렸다. 기존 Gem에는 자동 반영되지 않으므로 Studio에서 v7을 다시 복사해 교체해야 한다.
- taxonomy 검사에 모든 세부 장르의 상위 장르 정확히 1개 배정 및 별칭 충돌 검사를 추가했다.

**기존 데이터의 명백한 적용 항목**

- Kings of Convenience의 비어 있던 taxonomy를 `인디 포크·포크 팝·챔버 팝`으로 채우고, 잘못된 슈게이즈 대표 분류를 클래식/루츠 록으로 바로잡았다.
- Mumford & Sons에 인디 포크, Weezer에 파워 팝, Rage Against the Machine에 랩 록·훵크 메탈, Linkin Park에 랩 록을 추가했다.
- 해석이 필요한 밴드는 일괄 변경하지 않았다.

**검증**

- Vitest 11개 파일, 64개 테스트 통과.
- `validate:data`: 187개 밴드, 무결성 오류 없음.
- `validate:taxonomy`: 상위 장르 13개, 세부 장르 140개, 분위기 24개, 187/187 통과.
- 프로덕션 빌드와 공개 상세·공유 페이지 165개 생성 검증 통과.
- 이번 변경은 아직 커밋·푸시·배포하지 않았다.

## 22. 2026-08-02 히치하이킹 방향 정확도 · 모바일 상세 UI · 누적 인테이크 배포

**히치하이킹 방향 노출 규칙**

- 9개 방향마다 `triggerMoodIds`를 별도로 두었다. 보조 분위기는 후보 정렬에만 쓰고 현재 밴드의 방향 카드를 여는 근거로는 사용하지 않는다.
- 현재 밴드가 해당 방향의 핵심 분위기에서 3/5 이상일 때만 카드가 열린다. 따라서 Radiohead의 `전자음과 신스가 많은 4점`은 그루비 후보 정렬을 보조할 수 있어도 `춤추기 좋고 그루비한 0점`을 대신해 `더 그루비하게`를 열 수 없다.
- 후보도 같은 핵심 분위기 3/5 이상이며 장르·세부 장르·검수 관계 중 하나로 자연스럽게 이어져야 한다.
- 일반 밴드는 현재보다 방향 점수가 0.2 이상 높은 후보만 보여준다. 이미 핵심 분위기가 4~5점인 밴드는 더 높은 점수가 존재하기 어려우므로, 같은 핵심 성향 3점 이상인 자연스러운 연결을 옆으로 탐험할 수 있다.
- 방향별 후보가 3개 미만이면 그 방향 카드를 숨긴다. 최대 4개 방향만 핵심 점수와 종합 방향 점수 순으로 노출한다.
- 분위기 커버리지 진단도 보조 가중치가 아닌 `triggerMoodIds` 기준으로 통일했다.

**공개 상세 UI**

- 모바일 히치하이킹 후보 카드는 세로로 잘린 작은 사진을 없애고 16:9 가로 사진을 위, 설명을 아래에 배치했다. 모바일에서는 `object-fit: contain`으로 사진 전체 식별을 우선한다.
- `검수된 연결을 통한 경로` 문구를 삭제했다.
- 상세페이지 맨 아래의 `출처 · 권리 / 검수 n/n` 운영 배너를 공개 화면에서 완전히 삭제했다. 이미지 출처 링크와 곡·공식 채널 링크는 각 콘텐츠 위치에 유지한다.

**작업 중 발견한 차단 오류**

- 새로 추가된 The Doors가 이미지 파일명만 있고 권리 검수 상태가 끝나지 않은 채 공개되어 무결성 검사가 실패했다.
- Wikimedia Commons 공식 파일 메타데이터에서 Joel Brodsky/Elektra Records, Public domain 상태와 원본 URL을 확인해 검수 완료 정보와 Commons 출처를 보완했다.

**최종 검증**

- Vitest 11개 파일, 66개 테스트 통과. Radiohead 그루비 방향 비노출과 모든 방향의 핵심 분위기 3점 진입 조건을 회귀 테스트로 고정했다.
- 전체 공개 밴드 171개 모두 최소 1개 방향, 노출 방향마다 서로 다른 후보 3개를 유지한다.
- 분위기 24/24 사용, 188개 밴드 모두 핵심 분위기 보유. 엄격한 핵심 기준 후보는 방향별 24~141개다.
- `validate:data`: 188개 밴드, 573곡, 315개 관계, 무결성 오류 없음.
- `validate:taxonomy`: 상위 장르 13개, 세부 장르 140개, 분위기 24개, 188/188 통과.
- 프로덕션 빌드: 공개 상세 JSON·공유 페이지 171개 생성, 메인 전체 카탈로그 선로딩 없음 검증 통과.
- 실제 390px 화면에서 추천 카드 263×147px 가로 사진, Radiohead 그루비 카드 미노출, 두 삭제 요소 부재, 브라우저 오류 0건을 확인했다.

**Git 상태와 정확한 다음 단계**

- 이 섹션까지의 누적 인테이크 검수함, Gemini Gem v7, 세부 장르 140개 확장, 데이터 수정, 히치하이킹/UI 변경을 한 커밋으로 main에 배포한다.
- `.claude/settings.local.json`은 개인 설정 파일이므로 커밋에서 제외한다.
- 다음 데이터 운영 단계는 검수 경고 117개 중 직접 영상 검색 링크 66곡, 공식 YouTube 채널 누락 4개, 이미지 검수 대기 3개, 장르 변화 설명 보완 대상을 Studio에서 순차 처리하는 것이다. 모두 현재 배포를 막는 오류는 아니다.

## 23. 2026-08-02 관계 역방향 누락 전량 해소 · 종류 불일치 수정 · 트랙 영상 18곡 보완

공개 전 데이터 감사에서 최우선으로 지목된 관계 그래프의 비대칭(역방향 누락 191건, 종류 불일치 6건)을 사실관계 확인을 거쳐 전량 해소했다. 기계적 미러링이 아니라 각 쌍의 실제 이력을 확인해 근거를 남겼다.

**종류 불일치 6건 재분류**

- `foo-fighters → nirvana`, `megadeth → metallica`, `beady-eye → oasis`, `new-order → joy-division`: `sounds-like`/`influenced-by` → `evolution`으로 정정. 모두 원 밴드 해체·멤버 이탈 후 남은 멤버가 새 밴드를 결성한 직접 후신 관계다.
- `chickenfoot → red-hot-chili-peppers`(드러머 채드 스미스 겸업), `slash-feat-myles-kennedy-the-conspirators → alter-bridge`(보컬 마일스 케네디 겸업): `evolution` → `shared-scene`으로 정정. 두 경우 모두 멤버가 두 밴드에 동시 소속되어 있어 후신 관계가 아니다.
- `travis → snow-patrol`: `sounds-like` → `influenced`로 정정 (Travis가 Snow Patrol 초기 사운드에 영향을 준 시기적 인과관계이지 상호 유사 관계가 아님).

**판단이 갈릴 수 있는 케이스 — 사용자 확인 권장**

- `slash-feat-myles-kennedy-the-conspirators → guns-n-roses`: 슬래시가 1996년 GNR을 탈퇴해 이후 다른 프로젝트(Slash's Snakepit, Velvet Revolver, 현 Conspirators)를 거쳤다가 2016년 GNR 재결합에 합류했고, 재결합 이후에도 Conspirators를 계속 병행 중이라는 점에 근거해 `evolution` 대신 `shared-scene`으로 판단했다. 이력이 복잡한 만큼 이 판단이 맞는지 검토를 원할 수 있다.
- 나머지 185개 쌍은 기존 관계의 `kind`가 이미 정확했으므로 반대 방향에 `inverseKind()` 로직(`influenced-by`↔`influenced` 전환, 나머지 kind 동일 유지)으로 역방향만 채웠다. 이 중 13개 쌍(Racer X↔Mr. Big, Racer X↔Judas Priest, Oasis↔Noel Gallagher's High Flying Birds, The Police↔Sting, Oasis↔Ride 등)은 노트를 각 방향에 맞게 새로 작성했고, 나머지 172개 쌍은 원본 노트를 그대로 재사용했다(대칭적 서술이라 방향이 바뀌어도 사실관계가 동일하게 성립함을 확인).
- 애매해서 보류한 쌍은 없다. 모든 항목을 사실 근거로 해소했다.

**트랙 영상 링크 18곡 보완**

- 공식 YouTube 채널 영상이 없어 검색 링크로 대체되어 있던 18곡(Massive Attack 3곡, Epica 3곡, Nightwish 3곡, Cannibal Corpse 3곡, R.E.M. 3곡, Chickenfoot 3곡)에 실제 공식 채널 영상 ID를 찾아 반영하고 `reviewStatus`를 갱신했다.

**검증**

- `npm run audit:relations`: 관계 520개(이전 대비 191개 증가), 출처 연결 137개, 역방향 누락 0 · 종류 불일치 0 (이전 191/6).
- `npx tsc -b`, `npm test`(11개 파일 67개 테스트 통과), `npm run validate:taxonomy`(194/194 통과), `npm run validate:data`(무결성 오류 없음, 경고 44개 — 모두 이번 작업과 무관한 기존 장르 변화 설명 보완 권장), `npm run build`(공개 상세 193개·공유 페이지 193개 생성 검증 통과) 모두 통과.

**Git 상태와 다음 단계**

- 이 섹션의 관계 백필과 트랙 영상 수정을 커밋해 `codex/taxonomy-v2`에 푸시하고 `main`으로 배포한다.
- 사용자에게 Slash/GNS 재분류 판단을 특히 알려 재검토 여부를 확인받는다.
- 저장소 루트에 있던 분석용 스크래치 파일 `missing-relations.json`은 커밋 전 삭제했다.

## 24. 2026-08-02 히치하이킹 9방향 의미 조건 정밀화

Travis에서 `떼창하고 싶은 공연장형 3점`만으로 `더 웅장하게`가 열리는 사례를 계기로 9개 방향의 진입 조건을 전면 재검토했다.

**원인과 구조 변경**

- 기존에는 방향마다 여러 핵심 분위기를 평등한 OR 조건으로 묶어, 인접하지만 의미가 다른 분위기 하나가 3점이면 카드가 열릴 수 있었다.
- 방향마다 `entryRules`를 두고 `직접 핵심 조건`과 `조건부 보조 조합`을 구분했다. 후보 밴드에도 같은 조건을 적용한다.
- 보조 분위기는 계속 방향 점수와 추천 순위를 다듬지만, 단독으로 의미가 다른 방향을 열 수 없다.
- 밴드별 예외나 이름 목록은 사용하지 않으므로 앞으로 추가되는 모든 밴드에 자동 적용된다.

**방향별 핵심 기준**

- 무겁게: 묵직함 3점, 또는 공격성 4점 + 리프/빠름/노이즈 동반.
- 몽환적으로: 몽환 3점, 또는 환각성 4점 + 장곡/느림/실험성 동반.
- 대중적으로: 밝음·희망 3점, 또는 떼창 4점 + 감성/그루브/밝음 동반.
- 실험적으로: 실험성 3점, 또는 테크니컬 4점 + 장곡/환각/노이즈 동반.
- 빠르게: 질주감 3점.
- 어둡게: 어둠·차가움 3점, 또는 우울 4점 + 어둠/차가움/노이즈 동반.
- 따뜻하게: 따뜻함·어쿠스틱 3점, 또는 감성 4점 + 희망/따뜻함/어쿠스틱 동반.
- 그루비하게: 그루브 3점.
- 웅장하게: 영화적 웅장함 3점, 또는 떼창 4점 + 묵직함/장곡 동반.

**확인된 결과**

- Travis의 `더 웅장하게`, Radiohead의 `더 그루비하게`, Children of Bodom의 테크니컬 점수만으로 열리던 `더 실험적으로`가 제거됐다.
- 공개 밴드 193개 모두 최소 한 개 방향을 유지하고, 노출 방향마다 서로 다른 후보 3개 이상을 유지한다.
- 엄격한 진입 기준 후보 풀은 무겁게 53, 몽환 19, 대중 109, 실험 23, 빠름 81, 어둠 40, 따뜻함 49, 그루브 45, 웅장함 44개다.
- Vitest 11개 파일 69개 테스트, 데이터 194개 무결성 검사, taxonomy 194/194, 분위기 커버리지, 공개 상세·공유 페이지 193개 빌드가 모두 통과했다.

**Git 상태와 다음 단계**

- 이번 히치하이킹 변경은 아직 커밋·푸시·배포하지 않았다.
- 기존 사용자 변경 `docs/GEMINI_STEP1_RESEARCH.md`, `.claude/settings.local.json`, `배포법.txt`는 건드리거나 포함하지 않는다.
- 운영자가 결과를 승인하고 배포를 요청하면 히치하이킹 관련 파일과 이 상태 기록만 별도 커밋한다.
